---
 - hosts: all
   connection: local
   gather_facts: no
   become: yes
   become_method: enable

   tasks:
     - name: clear
       ios_command:
         commands:
          - mac address-table notification mac-move
          
          
