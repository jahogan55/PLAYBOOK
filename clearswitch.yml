---
 - hosts: all
   connection: local
   gather_facts: no
   become: yes
   become_method: enable

   tasks:
     - name: clear
       ios_command:
         commands:
          - configure terminal
          - mac address-table notification mac-move
          - clear mac address-table dynamic
          - clear arp-cache
          
          
