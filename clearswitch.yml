---
 - hosts: SWITCHES
   connection: local
   gather_facts: no
   become: yes
   become_method: enable

   tasks:
     - name: 
       asa_command:
         commands: 
          - command: "clear xlate"
          - command: "clear arp inside"
          - command: "clear mac-address-table"
