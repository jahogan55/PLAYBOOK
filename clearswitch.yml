---
 - hosts: SWITCHES
   connection: local
   gather_facts: no
   become: yes
   become_method: enable

   tasks:
     - name: clear stp and routing
       network_cli:
         commands: 
          - command: "clear xlate"
          - command: "clear arp inside"
          - command: "clear mac-address-table"
