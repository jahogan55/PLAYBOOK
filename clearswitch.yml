---
 - hosts: switches
   connection: local
   gather_facts: no
   become: yes
   become_method: enable

   tasks:
     - name: clear stp and routing
       ios_config:
         lines: 
          - show ver
          
